//We will ignore this Interface as because we have created a not Interface in Model with name iproperty and ipropertybase
export interface IProperty {
    Id: number;
    SellRent: number;
    Name: string;
    Type: string;
    Price: number;
    Image?: string;
}
